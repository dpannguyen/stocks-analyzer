{
  "name": "stonks-analyzer",
  "version": "1.0.0",
  "description": "Stonks Analyzer",
  "scripts": {
    "postinstall": "cd server && cd apiGateway && npm install && cd .. && cd marketIndex && npm install && cd .. && cd topTenStocks && npm install && cd .. && cd twitterSentiments && npm install && cd .. && cd .. && cd frontend && npm install  && cd .."
  }
}