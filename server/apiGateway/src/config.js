const config = {
    ibm: {
        endpoint: "{ibm.endpoint}",
        apiKeyId: "{ibm.apikey}",
        serviceInstanceId: "{ibm.serviceid}",
        signatureVersion: '{ibm.signature}',
        bucketName: '{ibm.bucket}'
    },
    port: 4444
};

module.exports = config;